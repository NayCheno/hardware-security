# Next steps

1. Build with `make check` after each direction update and inspect `main.pdf` locally.
2. Keep every primary paper as a fixed seven-slide mini-story generated by `\PaperStory`.
3. Add figures only after checking copyright/source and ensuring they are necessary.
4. Put concrete numbers in “实验结果” only after checking the original local PDF or official source.
5. Keep a per-direction summary slide: 技术演进、优点、缺口、商业化适配场景.
6. Keep `main.pdf` as a generated local QA artifact; do not commit it.
