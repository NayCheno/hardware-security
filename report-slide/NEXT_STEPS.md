# Next steps

1. Build once with `make` and inspect `main.pdf`.
2. For each direction, split every current `\PaperFrame` into a 5--7 slide mini-story.
3. Add figures from each paper only after checking copyright/source and ensuring they are necessary.
4. Move verified numbers from original PDFs into the “实验结果” slides; keep “以原文为准” placeholders until checked.
5. Add a per-direction summary slide: 技术演进、优点、缺口、商业化适配场景.
6. Add CI: run xelatex and fail on missing files or undefined references.
