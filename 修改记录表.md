# 修改记录表

| 文件 | 页码/Slide | 问题类型 | 原问题 | 修改动作 | 修改后效果 | 是否需人工复核 |
| --- | --- | --- | --- | --- | --- | --- |
| `report-slide/config/preamble.tex` | 全 deck | 页面过密 / 图示缺失 | 旧主题颜色和字体偏松散，缺少统一证据 badge 与紧凑来源脚注 | 统一 16:9 Beamer 主题、学术技术报告配色、字体回退、`EvidenceBadge`、`SlideMeta`、`SourceFootnote`、`OverviewPanel` | PDF 页面有统一标题区、证据标签、页脚来源和页码；避免 Aptos 缺失导致 LaTeX 字体报错 | 否 |
| `report-slide/main.tex` | Slide 1 | 封面弱 / 信息结构不足 | 默认 title page 信息层次弱，缺少报告类型、核心指标和技术主线 | 重做封面，加入 Research Slide Report、15/45/260/E0-E5 指标和五段式技术路线 | 封面更像正式研究型 slide report，并保留原主题与副标题 | 否 |
| `report-slide/tools/generate_beamer.py` | Slides 2-4、各方向页、材料页 | 页面过密 / 页脚冗长 | 前 4 页过密，正文页脚重复长句，实验图表页局部触发 shrink 警告 | 重写总览、证据等级、方向矩阵模板；压缩 source note；降低 metric bars 垂直间距；为正文页增加紧凑来源脚注 | 前 4 页变为卡片、表格、矩阵；PDF 无 vbox 溢出和 shrink 超阈值警告 | 否 |
| `report-slide/pptx/build.mjs` | PPTX 全 deck | PDF/PPTX 风格不一致 | PPTX 色彩、页脚、证据标签与 Beamer 模板不完全一致 | 同步配色、封面指标、证据 badge、meta/source note 和前 4 页布局文案 | `out/report.pptx` 与 PDF 的视觉系统和证据边界表达保持一致 | 否 |
| `report-slide/tools/generate_story_yaml.py` | 02-15 `story.yml` | 文字截断 / 引用页脚冗长 | 自动摘要可能截断英文 token，source note 模板过长 | `shorten()` 避免在英文词中间截断；`source_note()` 改为 `来源：...｜证据等级：...` | 生成的 story 页脚更短，英文断词残留减少 | 否 |
| `report-slide/*/story.yml` | 02-15 方向正文页 | 文字截断 / 乱码断词 / 术语不统一 | 存在 `attestationon`、`semanticsntics`、`confi dential`、`结论s`、半截英文等问题 | 重新生成并清理 story；修正 `Penglai` 和 `CoVE-IO` 两处新发现截断 claim | 当前缺陷搜索无命中，术语统一为 Arm / RISC-V、CoVE / AP-TEE、PCIe IDE、CXL.mem、SmartNIC、Trusted I/O、TEE-I/O 等 | 否 |
| `report-slide/*/papers.yml` | 实验页源数据 | 实验数字待补 / 占位句 | 部分实验页使用“具体数值以本地 PDF 为准”类占位或不完整 claim | 改为“待补证据：需要核对原文 Table/Figure 后再写入具体数值/指标/数字”，并修正截断 claim | 未核验数字不被编造，页面明确标出人工复核项 | 是 |
| `report-slide/00-overview/section.tex` | Slides 2-4 | 页面过密 / 表格不可读 | 报告定位、证据等级、方向总览信息密度高 | 由生成器重建为三卡片、E0-E5 方法论表、15 方向分组矩阵 | 前 4 页信息层级更清晰，保留证据等级体系和 15 方向结构 | 否 |
| `report-slide/<direction>/section.tex` | Slides 5-259 | 引用页脚冗长 / 模板化页面 | 方向页、材料页来源说明冗长且视觉重复 | 由更新后的 Beamer 生成器统一重建 section | 每页保持统一 badge、来源脚注和卡片/表格/流程化布局 | 否 |
| `out/report.pdf` | 1-260 | PDF 导出问题 | 需从源文件统一导出，不手工编辑最终 PDF | 运行 `uv run build report` 生成 | 输出 260 页 PDF，日志无 vbox 溢出、无 shrink 超阈值、无字体加载失败 | 否 |
| `out/report.pptx` | 1-260 | PPTX 导出问题 | 需与 PDF 同源生成且页数一致 | 运行 `uv run build report` 生成 | PPTX manifest 显示 260/260 张，写入 `out/report.pptx` | 否 |
