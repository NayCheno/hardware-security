# Hardware Security Survey Slide 优化计划

## 目标
优化 `report-slide/` 中幻灯片的**风格、设计与整体呈现效果**，使其作为技术 survey 更加专业、清晰、有视觉冲击力，同时保持内容完整性与证据严谨性。

## 现状诊断

### 1. 视觉问题
- **布局单一**：260+ 页几乎使用相同的左右分栏模板，视觉疲劳严重
- **信息密度过高**：大量文字堆砌，不适合演讲场景
- **色彩保守**：以白/浅灰为主，蓝色 accent 使用不够大胆，Secondary 色几乎未使用
- **字体层级混乱**：过度使用 `\scriptsize`/`\tiny`，可读性差
- **缺少视觉元素**：无图标、无进度指示、无章节过渡

### 2. 结构问题
- **缺少章节过渡页**：15 个方向之间没有清晰的分隔
- **页面类型无区分**：摘要/背景/方案/实验/评价使用相同布局
- **Footer 过于简单**：只有页码和来源，缺少方向进度指示

## 优化方案

### Phase 1: 核心视觉系统升级
**目标**：升级配色、字体、间距系统

1. **配色升级**
   - 背景：从纯白 `#F8FAFC` 改为更温暖的极浅灰 `#FAFBFC`，增加纸张质感
   - 主色：保持蓝色 `#2563EB`，但增加深蓝色 `#1E40AF` 作为标题强调
   - 新增：渐变蓝色条（用于标题区域和章节页）
   - 辅助色：青色 `#0891B2` 用于次要信息，橙色 `#F59E0B` 用于警告/证据边界
   - 文字色：增加层次感——Ink `#0F172A`（标题）、Soft `#475569`（正文）、Muted `#64748B`（注释）

2. **字体系统升级**
   - 标题：更大、更粗，使用深蓝色
   - 正文：增加行高，使用 14-16pt 而非 10-12pt
   - 注释：保持 9-10pt，但与正文对比度提高

3. **间距系统**
   - 增加页面边距（从 58px 到 64px）
   - 增加元素间距，减少拥挤感
   - 统一使用 8px 网格系统

### Phase 2: 布局模板多样化
**目标**：为不同页面类型设计专属模板

1. **封面页**（Cover）
   - 左侧添加渐变蓝色竖条（24px 宽）
   - 标题放大，使用深蓝色
   - 副标题使用灰色，增加间距
   - Metric cards 改为更现代的卡片设计（带阴影而非边框）
   - 右侧视觉面板改为圆角卡片

2. **新增：章节过渡页**（Section Divider）
   - 每个方向开始前插入一页
   - 左侧大面积渐变蓝色块（占 1/3 宽度）
   - 方向编号使用超大号字体（72pt+）
   - 方向名称使用粗体标题
   - 简要描述本方向聚焦点
   - 底部显示主讲论文列表

3. **总览页**（Overview）
   - 改进 evidence 等级展示：从简单表格改为卡片式时间线
   - 方向索引：使用图标化卡片，增加颜色编码

4. **内容页**（Paper Slide）
   - **摘要页**：左侧使用蓝色主题色，强调核心贡献
   - **背景页**：左侧使用灰色主题，强调问题边界
   - **方案页**：左侧使用绿色主题，强调解决方案
   - **实验页**：左侧使用金色主题，强调数据证据
   - 改进布局：将固定左右分栏改为动态布局
   - 增加左侧彩色竖条（4px）标识页面类型

5. **评价页**（Evaluation）
   - 三列卡片使用不同顶部色条（优点=绿色，不足=红色，商业=金色）
   - 卡片改为轻微阴影而非边框
   - 增加图标或标识

6. **总结页**（Direction Summary / Final）
   - 使用时间线式布局展示技术演进
   - 增加连接线或流程箭头

### Phase 3: 全局视觉增强
**目标**：增加全局一致性元素

1. **左侧进度指示条**
   - 每页左侧边缘添加 4px 色条，颜色随方向变化
   - 章节页使用完整左侧色块

2. **改进 Footer**
   - 左侧：当前方向名称 + 当前页面类型
   - 中间：全局进度（如 "01 / 15 方向"）
   - 右侧：页码，使用更大字体

3. **改进 Evidence Badge**
   - 从简单边框改为圆角 pill 形状
   - 颜色根据证据等级变化（E0=深蓝, E1=蓝, E2=绿, E3=橙, E4=灰, E5=红）

4. **改进 Kicker**
   - 从简单色块+文字改为带图标的标签
   - 增加与页面类型的关联（如背景页使用灰色图标）

### Phase 4: PPTX 构建脚本优化
**目标**：在 `build.mjs` 中实现上述改进

1. **STYLE 对象升级**：添加新颜色和字体配置
2. **新增辅助函数**：
   - `gradientBar()` - 渐变条
   - `sectionDivider()` - 章节过渡页
   - `coloredKicker()` - 彩色标签
   - `progressBar()` - 进度条
   - `shadowPanel()` - 阴影卡片
3. **重构各页面类型渲染函数**
4. **增加全局装饰元素**（每页自动添加）

### Phase 5: LaTeX 主题同步优化
**目标**：`preamble.tex` 与 PPTX 视觉保持一致

1. 升级 Beamer 颜色主题
2. 增加章节过渡页模板
3. 改进 frame 标题样式
4. 增加 `─` 装饰线分隔符

## 执行顺序
1. 先修改 `build.mjs`（PPTX 是主要交付物）
2. 构建并预览效果
3. 根据预览调整
4. 同步修改 `preamble.tex`
5. 最终验证

## 风险与约束
- 不能改变内容结构（YAML 内容源不变）
- 不能改变证据等级标注
- 需要保持与现有构建流程的兼容性
- 颜色方案需符合低饱和度、暖色调要求（避免蓝紫渐变）
